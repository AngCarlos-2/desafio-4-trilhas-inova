using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameOver : MonoBehaviour
{
    // public Player[] personagens;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /*int index = PlayerPrefs.GetInt("PersonagemSelecionado", 0);
        if (personagens[index].gameOverValidation != null)
        {
            if (personagens[index].gameOverValidation.Gameover)
            {
                personagens[index].isPlaying = false;

                if (personagens[index].distanciaMax > maiorDistancia)
                {
                    maiorDistancia = distanciaMax;
                    PlayerPrefs.SetInt("MaiorDistancia", maiorDistancia);
                    PlayerPrefs.Save();

                }
            }
        }*/
    }
}
